export class PostListItem {
    constructor(userId, postId, title, excerpt, userName) {
        this.userId = userId;
        this.postId = postId;
        this.title = title;
        this.excerpt = excerpt;
        this.userName = userName;
    }
}